<UserControl x:Class="TimeRenderer.Views.CalendarGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:TimeRenderer.Controls">
    <!-- 月ビュー / スプリントビュー共通のカレンダーグリッド（曜日ヘッダー + セル） -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/> <!-- 曜日ヘッダー -->
            <RowDefinition Height="*"/>  <!-- 日付セル -->
        </Grid.RowDefinitions>

        <!-- 曜日ヘッダー -->
        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <ItemsControl ItemsSource="{Binding DataContext.EnabledDayHeaders, RelativeSource={RelativeSource AncestorType=Window}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1" Background="{DynamicResource BackgroundBrush}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" Height="30" LineHeight="30">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding DayOfWeek}" Value="Saturday">
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding DayOfWeek}" Value="Sunday">
                                            <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- カレンダーのセル -->
        <controls:TransitioningContentControl Grid.Row="1" Content="{Binding CurrentDate}" TransitionDirection="{Binding TransitionDirection}"
                                              TransitionAxis="Vertical"
                                              HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
            <controls:TransitioningContentControl.ContentTemplate>
                <DataTemplate>
                    <ItemsControl ItemsSource="{Binding DataContext.CalendarCells, RelativeSource={RelativeSource AncestorType=Window}}">
                        <ItemsControl.CacheMode>
                            <BitmapCache EnableClearType="True" RenderAtScale="1"/>
                        </ItemsControl.CacheMode>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="{Binding Rows, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             Columns="{Binding DataContext.EnabledDaysCount, RelativeSource={RelativeSource AncestorType=Window}}"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <controls:CalendarMonthCellControl CellData="{Binding}"
                                                                   MutedBackgroundBrush="{DynamicResource MutedBackgroundBrush}"
                                                                   TextSecondaryBrush="{DynamicResource TextSecondaryBrush}"
                                                                   TextPrimaryBrush="{DynamicResource TextPrimaryBrush}"
                                                                   TodayBackgroundBrush="{DynamicResource PrimarySubtleBrush}"
                                                                   SundayForegroundBrush="{DynamicResource DangerBrush}"
                                                                   SaturdayForegroundBrush="{DynamicResource PrimaryBrush}"
                                                                   WeekdayForegroundBrush="{DynamicResource TextPrimaryBrush}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DataTemplate>
            </controls:TransitioningContentControl.ContentTemplate>
        </controls:TransitioningContentControl>
    </Grid>
</UserControl>
